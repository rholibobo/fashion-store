import { createTheme } from "@mui/material/styles";

const theme = createTheme({
 palette: {
   primary: {
     main: "#fcba03",
   },
 },
 typography: {
    fontFamily: "inherit",
  },
});

export default theme;