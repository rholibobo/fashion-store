import { createTheme } from "@mui/material/styles";

const theme = createTheme({
 palette: {
   primary: {
     main: "#fcba03",
     badge: "#CE5A67"
   },
 },
 typography: {
    fontFamily: "inherit",
  },
});

export default theme;